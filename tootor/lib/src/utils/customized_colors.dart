import 'package:flutter/material.dart';

class CustomColors {
  static const Color primary = Color.fromARGB(255, 35, 55, 77);
  static const Color secondary = Color.fromARGB(255, 16, 137, 255);
  static const Color gray = Color.fromARGB(255, 229, 229, 229);
  static const Color light_gray = Color.fromARGB(255, 238, 238, 238);
  static const Color font_gray = Color(0xFF47525E); // Used in "Cancelar, Rechazar, Omitir" and in subjects name
  static const Color divider_gray = Color(0xFF343F4B); // Used for each divider
  static const Color red = Color(0xFFFB0D12); //Used in Sign out
}